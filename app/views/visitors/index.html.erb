<div id="notice"><%= notice %></div>

<section class="about">
  <h2>About Section</h2>
  <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quibusdam, quae, voluptates, odio, adipisci porro voluptas vel id eaque explicabo fugiat enim animi aperiam recusandae placeat veniam debitis molestiae voluptate repudiandae.</p>
</section>

<% @combined_proposals_courses.each do |c| %>
<a href="<%= proposal_course_pth(c) %>">
  <section class="post">
  <h1><%= c.class.to_s %></h1>
  <h1><%= c.name %></h1>
    <p><%= truncate(c.description, :length => 120) %></p>
    <p class="votes">Votes: <%= c.votes.count %></p>
  </section>
</a>

<% end %>

<%= paginate @combined_proposals_courses %>
